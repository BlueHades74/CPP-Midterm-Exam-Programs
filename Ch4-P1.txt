//  Chapter 4, Program 1

#include <iostream>
#include <string>

using namespace std;

int main() {
    
    string name1, name2;
    int age1, age2;
    char grade;
    
    cout << "Enter your name and the name of your friend: ";
    cin >> name1 >> name2;
    
    cout << "Enter your age and the age of your friend: ";
    cin >> age1 >> age2;
    
    cout << "Finally, enter the grade you got in C++: ";
    cin >> grade;
    
    if (age1 >= 21 && age2 >= 21){
        switch (grade){
            case 'F':
            case 'D':
                cout << "Maybe less partying and more studying next time.";
                break;
            case 'C':
            case 'B':
                cout << "Hey, not bad. You passed and that's the important part.\n"
                    << "Maybe go out to a bar or club with your friend to celebrate";
                break;
            case 'A':
                cout << "Alright, show off. Maybe get out more, and take your friend with you.";
                break;
        }
    } else if (age1 < 21 || age2 < 21){
        switch (grade){
            case 'F':
            case 'D':
                cout << "How? You and your friend couldn't even go out to party.";
                break;
            case 'C':
            case 'B':
                cout << "Hey, not bad. You passed and that's the important part.\n"
                    << "Maybe go get you and your friend a caprisun to celebrate";
                break;
            case 'A':
                cout << "Alright, show off. Maybe get out more, and take your friend with you.";
                break;
        }
    }
        
    
    
    return 0;
}